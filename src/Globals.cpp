#include "Globals.h"

//float length(Vector2f v) {
//	return sqrtf(v.x*v.x + v.y*v.y);
//}
//
//Vector2f rotate(Vector2f v, float r) {
//	v.x = v.x * cosf(r) + v.y * -sin(r);
//	v.y = v.y * sinf(r) + v.y * cos(r);
//	return v;
//}